#exercise #solution

**Exercise 5.18.** Draw the monoidal product of the port graph (5.15) with itself.

## Solution

Stack the picture on top of itself: a $(4, 6)$-port graph with boxes $a, b, c, a', b', c'$ and no wires between the two copies. See [[Port Graph]].
