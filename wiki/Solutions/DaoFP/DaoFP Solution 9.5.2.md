#solution #program

**Solution to [[DaoFP Exercise 9.5.2|Exercise 9.5.2]].**

`q' x` is `True` iff `x` is odd; `h q' (q x)` returns `q' 0 = False` when `x` is even and `q' 1 = True` when odd. Equal for all `x`. See [[Coequalizer]].

> Sources: DaoFP Exercise 9.5.2.
