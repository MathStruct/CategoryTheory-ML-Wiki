#exercise #solution #proof

**Exercise 5.83.** Show $B(g^{\mathrm{op}} \mathbin{;} h) = \{(S(g)x, S(h)x) \mid x \in R^m\}$.

## Solution

$B(g^{\mathrm{op}}) = \{(y, x) \mid y = S(g)x\}$, $B(h) = \{(x, z) \mid S(h)x = z\}$; composing over the middle $x$ gives $\{(S(g)x, S(h)x)\}$.
