#exercise #solution

**Exercise 3.21.** Which equations turn the graphs $G_1$ (two parallel arrows $f, g$), $G_2$ (a loop $f$), $G_3$ (a square $f, g, h, i$), $G_4$ (two arrows out of one vertex) into presentations of the associated preorders?

## Solution

$G_1$: $f = g$. $G_2$: $f = \mathrm{id}$ ($f = a$ where $a$ is the identity path). $G_3$: $f \mathbin{;} h = g \mathbin{;} i$. $G_4$: no equations — there are no parallel paths. A [[Hasse Diagram]] presents a preorder by equating all parallel paths.
